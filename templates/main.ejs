<% layout('layout/page') -%>
<% block('barMain', 'active') -%>

<div class="row main">
  <div class="left col-md-9 news">
      <div class="adm <% if(role != "admin") {%> hidden <% } %>">
        <form class="btn" role="form" action="/editmain">
          <button type="submit" class="btn btn-default btn-xs" title="Редагувати" aria-label="Pencil">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
          </button>
        </form>
      </div>
    <div class="">
      <%- main %>
    </div>
  </div>
  <% include layout/login %>
</div>
